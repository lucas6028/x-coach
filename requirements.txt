# x-coach Python dependencies for the current .venv
# Generated from the repo's direct imports and active environment packages.

# Web backend (FastAPI service under backend/)
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18
pydantic-settings==2.14.1
supabase>=2.0,<3

numpy==2.4.3
opencv-python==4.13.0.92
mediapipe==0.10.14
tqdm==4.68.3
networkx==3.6.1
pydantic==2.12.5
langchain==1.2.10
langchain-core==1.2.28
langchain-community==0.4.1
langchain-google-genai==4.2.1
langchain-openrouter==0.2.1
openrouter==0.8.1
langchain-text-splitters==1.1.1
pypdf==6.8.0
beautifulsoup4==4.15.0
torch==2.11.0
transformers==5.5.0
pytest>=8,<9
coverage>=7,<8

# Optional: RTMPose/MMPose-based pose extraction (src/pose/rtmpose_pose_extraction.py).
# Lazily imported and not installed in the base .venv; install only if you
# need the COCO-WholeBody / RTMPose path (rtmlib) or the MMPose runtime (HRNet).
# Versions left unpinned because the mmcv/mmpose stack is environment- and CUDA-specific.
# mmpose
# rtmlib
# pycocotools
